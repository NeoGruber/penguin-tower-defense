class_name TowerEnums; 
enum TowerMode {first, last, nearest, furthest, strongest}
enum TowerWeapon { Bow, Staff, Sword }
# bow just shoots a projectile
# staff does it too, but with an explosion or something
# sword is something lake a swep
# code hitbox / hurtbox first
